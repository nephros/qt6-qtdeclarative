diff --git a/src/qml/CMakeLists.txt b/src/qml/CMakeLists.txt
index 2763f2187..e5149e085 100644
--- a/src/qml/CMakeLists.txt
+++ b/src/qml/CMakeLists.txt
@@ -472,10 +472,14 @@ add_custom_command(
         "${qml_root_dir}/builtins.qmltypes"
     DEPENDS
         "${qml_build_dir}/plugins.qmltypes"
+    COMMAND
+        chmod -f 777 "${qml_build_dir}/plugins.qmltypes"
     COMMAND
         ${CMAKE_COMMAND} -E copy_if_different
         "${qml_build_dir}/plugins.qmltypes"
         "${qml_root_dir}/builtins.qmltypes"
+    COMMAND
+        chmod -f 777 "${qml_root_dir}/builtins.qmltypes"
     COMMENT "Copying builtins.qmltypes"
     VERBATIM
 )
@@ -485,10 +489,14 @@ add_custom_command(
         "${qml_root_dir}/jsroot.qmltypes"
     DEPENDS
         "${qml_build_dir}/jsroot.qmltypes"
+    COMMAND
+        chmod -f 777 "${qml_build_dir}/jsroot.qmltypes"
     COMMAND
         ${CMAKE_COMMAND} -E copy_if_different
         "${qml_build_dir}/jsroot.qmltypes"
         "${qml_root_dir}/jsroot.qmltypes"
+    COMMAND
+        chmod -f 777 "${qml_root_dir}/jsroot.qmltypes"
     COMMENT "Copying jsroot.qmltypes"
     VERBATIM
 )
